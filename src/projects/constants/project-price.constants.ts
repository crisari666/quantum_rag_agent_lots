/** Primary project list price and lot `price` are stored in COP. */
export const PROJECT_PRIMARY_PRICE_CURRENCY = 'COP' as const;

/** Optional parallel amounts use USD (`priceSellUsd`, `lotOptions[].priceUsd`). */
export const PROJECT_REFERENCE_PRICE_CURRENCY = 'USD' as const;
